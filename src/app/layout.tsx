import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Portfolio Dashboard | Real-time Stock Tracking",
  description: "A dynamic portfolio dashboard displaying real-time stock prices, P/E ratios, and earnings data from Yahoo Finance",
  keywords: ["portfolio", "stocks", "finance", "dashboard", "investment", "NSE", "BSE"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
